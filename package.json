{
  "name": "dicebox",
  "version": "1.0.0",
  "private": true,
  "description": "An offline dice roller for tabletop RPGs.",
  "type": "module",
  "scripts": {
    "test": "node tools/test.mjs && node tools/test-render.mjs && node tools/test-pool.mjs && node tools/test-markup.mjs && node tools/test-load.mjs",
    "icons": "node tools/make-icons.cjs",
    "preview": "node tools/preview.cjs",
    "serve": "python3 -m http.server 8080",
    "dev": "wrangler dev",
    "deploy": "npm test && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
